Refactor notification service and update configuration infrastructure

- Refactor `ldms-notifications` module:
    - Rename artifact and project name from `ldms-notifications-management-service` to `ldms-notifications`.
    - Simplify package structure from `projectlx.co.zw.notificationsmanagementservice` to `projectlx.co.zw.notifications`.
    - Rename main application class to `Notifications`.
- Update infrastructure and database configurations:
    - Update `docker-compose.yml` with MySQL credentials and adjust Mosquitto port from 1883 to 1884.
    - Rename audit trail database schema to `ldms_audit_trail_dev_db` in SQL migrations and YAML configs.
    - Synchronize application names and database connection strings across dev, staging, and prod configurations in `ldms-config-repo`.
- Backend adjustments:
    - Update POM files for `ldms-api-gateway`, `ldms-audit-trail`, `ldms-config-server`, `ldms-eureka-server`, and `ldms-shared-library`.
    - Adjust logging configurations (`logback-spring.xml`) for multiple services.
- Initialize `platform-portal` Angular project in `ldms-web/`.
