package projectlx.co.zw.audittrail.utils.enums;

/** Lifecycle state for a churn execution row (distinct from {@link projectlx.co.zw.shared_library.utils.enums.EntityStatus}). */
public enum AuditLogChurnStatus {
    RUNNING,
    SUCCESS,
    FAILED
}
