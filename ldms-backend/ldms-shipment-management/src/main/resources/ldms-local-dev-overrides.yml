# Loaded after Config Server — wins over ldms-config-repo for local LDMS port map.
server:
  port: 8015

management:
  server:
    port: 9015

# Keep JWT signing aligned with ldms-authentication when Config Server is slow/offline at startup.
jwt:
  secret: ${JWT_SECRET:dev-jwt-secret-change-me-please-override}
