package projectlx.co.zw.organizationmanagement.utils.enums;

/**
 * {@code OWNED} — asset owned and operated by {@code organization_id}.
 * {@code CONTRACTED} — asset recorded by the supplier and linked to a contracted transporter.
 */
public enum FleetVehicleOwnershipType {
    OWNED,
    CONTRACTED
}
