package projectlx.fleet.management.utils.enums;

/**
 * How a driver relates to fleet operations.
 * EMPLOYED — dedicated staff (often linked to an org user).
 * POOL — driver pool; assign to whichever truck is available.
 */
public enum DriverEmploymentType {
    EMPLOYED,
    POOL
}
