package projectlx.trip.tracking.utils.enums;

public enum TripStatus {
    SCHEDULED,
    IN_TRANSIT,
    AT_BORDER_HOLD,
    ROADSIDE_HOLD,
    ARRIVED,
    OTP_PENDING,
    DELIVERED,
    CANCELLED
}
