package projectlx.trip.tracking.utils.enums;

public enum TripStatus {
    SCHEDULED,
    IN_TRANSIT,
    AT_BORDER_HOLD,
    ROADSIDE_HOLD,
    ARRIVED,
    COUNTING_STOCK,
    COUNT_COMPLETE,
    OTP_PENDING,
    DELIVERED,
    RETURN_IN_TRANSIT,
    RETURNED,
    CANCELLED
}
