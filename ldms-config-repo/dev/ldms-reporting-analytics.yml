server:
  port: 8098

spring:
  application:
    name: ldms-reporting-analytics
  datasource:
    url: jdbc:mysql://localhost:3306/ldms_analytics?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false

management:
  server:
    port: 9098
