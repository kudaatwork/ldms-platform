server:
  port: 8091

spring:
  application:
    name: ldms-roadside-support
  datasource:
    url: jdbc:mysql://${DB_HOST:staging-db}:3306/ldms_roadside_support?useSSL=true&serverTimezone=UTC
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false

management:
  server:
    port: 9091
  tracing:
    sampling:
      probability: 0.1

logging:
  level:
    projectlx: INFO

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://staging-eureka:8761/eureka/}