server:
  port: 8090

spring:
  application:
    name: ldms-trip-tracking
  datasource:
    url: jdbc:mysql://${DB_HOST:staging-db}:3306/ldms_trip_tracking?useSSL=true&serverTimezone=UTC
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 30
      minimum-idle: 5
  jpa:
    show-sql: false
mqtt:
  broker-url: tcp://${MQTT_HOST:localhost}:1883
  client-id: ldms-trip-tracking-${random.int}
  username: ${MQTT_USERNAME:admin}
  password: ${MQTT_PASSWORD:admin}
  topic:
    gps-inbound: ldms/devices/+/gps
    gps-outbound-prefix: ldms/trips/
  qos: 1

clients:
  base-url:
    fleet-management: http://ldms-fleet-management
    shipment-management: http://ldms-shipment-management

ldms:
  trip:
    websocket:
      endpoint: /ws
      destination-prefix: /topic/trips
    gps:
      inactive-device-ttl-seconds: 86400

management:
  server:
    port: 9090
  tracing:
    sampling:
      probability: 0.1

logging:
  level:
    projectlx: INFO

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://staging-eureka:8761/eureka/}