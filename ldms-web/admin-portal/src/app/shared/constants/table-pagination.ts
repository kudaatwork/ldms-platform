/** Default rows per page for admin data tables (must match server request `size`). */
export const DEFAULT_TABLE_PAGE_SIZE = 20;

/** Standard page-size choices shown in table paginators. */
export const DEFAULT_TABLE_PAGE_SIZE_OPTIONS: number[] = [10, 20, 25, 50];
